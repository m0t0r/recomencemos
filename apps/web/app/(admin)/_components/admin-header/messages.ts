/**
 * The Admin shell's strings, in `es-CO`.
 *
 * The register is `admin/_lib/messages.ts`'s — Directness at 5, nothing explained
 * to somebody who works this queue daily. English identifiers, Spanish values
 * (ADR-0012).
 */

/**
 * Link text names its destination (`docs/policy/voice.md`), which for a wordmark
 * means the accessible name says where it goes rather than repeating the word.
 * It goes to the queue and not to the Wall — this shell's home is `/admin`.
 */
export const QUEUE_LINK_LABEL = "Ir a la fila";
