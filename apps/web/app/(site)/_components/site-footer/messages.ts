/**
 * Every `es-CO` string the footer renders, in one module — the rule the shell
 * set in `site-header/messages.ts`, for the same reason.
 *
 * The footer says three things: the name, where the product is for, and where
 * the privacy notice is. The earthquake, the three refusals and the About page
 * are story 11's and are deliberately absent here.
 */

/** The three municipalities, as a sentence rather than a dotted list. */
export const FOOTER_PLACES = "Para Pereira, Dosquebradas y Santa Rosa de Cabal.";

/**
 * Link text names its destination: the same words `/privacy` titles itself
 * with (`NOTICE_TITLE`), so the page a person lands on calls itself what the
 * link called it.
 */
export const FOOTER_PRIVACY = "Aviso de privacidad";
