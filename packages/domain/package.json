{
  "name": "@repo/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "imports": {
    "#config": "./src/config.ts",
    "#connection": "./src/connection.ts",
    "#migrate": "./src/migrate/index.ts",
    "#schema": "./src/schema.ts",
    "#server-only": "./src/server-only.ts",
    "#testing/database": "./src/testing/database.ts"
  },
  "exports": {
    "./health": "./src/health.ts",
    "./migrate": "./src/migrate/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate": "drizzle-kit generate"
  },
  "dependencies": {
    "@repo/errors": "workspace:*",
    "@repo/observability": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.7",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^24.13.3",
    "@types/pg": "^8.23.1",
    "drizzle-kit": "^0.31.10",
    "typescript": "7.0.2",
    "vitest": "^4.1.11"
  }
}
