# The local development database and object store: `docs/policy/data.md` →
# `local-database`.
#
# Two services rather than one, and the second one is the whole point.
# PlanetScale's pooler runs in **transaction-pooling** mode (spec 0002, DD2),
# which removes LISTEN/NOTIFY, session advisory locks, temp tables and
# cross-transaction prepared statements. Develop against a plain unpooled
# Postgres and it is possible to write code that passes locally and fails in
# production in exactly that class of way — so PgBouncer runs here too, in the
# same mode, and refuses locally what production refuses.
#
# Both of DD2's connection strings therefore exist and are distinguishable:
#
#   pooled  postgres://recomencemos:recomencemos@localhost:6432/recomencemos
#   direct  postgres://recomencemos:recomencemos@localhost:5432/recomencemos
#
# The app uses the pooled one; migrations, which are DDL in long transactions,
# use the direct one. `apps/web/.env.example` carries both.
#
# **This file is for `pnpm dev` and nothing else.** `pnpm test` runs against
# PGlite in-memory (spec 0002, Testing Decisions seam 2) and CI runs no database
# service at all. Docker is a requirement of the dev loop, not of the repository.
#
# **The credentials below are in git on purpose.** They are development-tier
# values that reach a container listening on loopback, and they are the same
# non-secret in `apps/web/.env.example` and `docker/pgbouncer/userlist.txt`. A
# production URL in any of the three is the incident C13 names — `.env*` is a
# Turborepo `build` input, so it would be one `git add` away.
#
# **The third service is an object store, and it is here for the same argument
# PgBouncer is** (#18, DD6). The photo path is a **presigned PUT** into a
# prefix the bucket refuses to serve — NFR6's _"**0** unmoderated photo
# **objects** are retrievable by an unauthenticated request"_ is a statement
# about that bucket policy rather than about our routing. A development loop
# with no bucket could only exercise it through a stand-in, and a stand-in
# cannot be wrong in the way a signature is wrong. Versity S3 Gateway speaks
# the same S3 protocol Cloudflare R2 does, so the code presigning here is the
# code presigning in production. It was picked from four candidates in #299:
# the storage suite passes against it unmodified, and #299's measurement found
# no deviation from R2 in either direction. One thing here is not R2's: a
# development-only CORS rule on quarantine, on `object-store-init` below.
#
# **`object-store-init` is what makes the negative test real.** It creates the
# two buckets and makes exactly one of them anonymously readable — photos,
# never quarantine. Fetching a quarantined object directly and being refused is
# criterion 1 of #18, and it is only a test if something is actually refusing.
#
# **The store's services and volume are named for their role, not their
# vendor** (#316), so the next swap is a change of image rather than a rename
# through scripts, docs and volumes. `pnpm db:up` passes `--remove-orphans`
# for the same reason: a container of a service this file no longer declares —
# a previous store's, on a machine that ran one — is removed rather than left
# holding port 9000.
#
# **Images are pinned by tag and digest, stamped at a date**, the way
# `docs/runbooks/observability-go-live.md` pins a vendor figure. Checked
# **2026-08-26**: `postgres:18-alpine` is PostgreSQL **18.6**, `edoburu/pgbouncer`
# `v1.25.2-p0` is PgBouncer **1.25.2**. Checked **2026-09-15**:
# `versity/versitygw` is `v1.8.0` and `curlimages/curl` is curl **8.22.0**,
# both the latest on Docker Hub at that date. Read from the registry and the
# images, not recalled. `.github/dependabot.yml` carries a `docker-compose`
# entry so the digests move on their own rather than freezing at whatever was
# current today.
#
# The engine version drifts three ways and it is worth knowing by how much:
# PlanetScale runs 18.4, PGlite 0.5.7 is 18.3, this is 18.6. One major version,
# which is what seam 2's argument rests on.

name: recomencemos

services:
  # The direct connection. DDL, long transactions and anything session-scoped.
  postgres:
    image: postgres:18-alpine@sha256:d3e1620b530c944afa6e887d22eb899824da68e19c52024bf98f5220c88a65b2
    environment:
      POSTGRES_USER: recomencemos
      POSTGRES_PASSWORD: recomencemos
      POSTGRES_DB: recomencemos
      # **Two settings, because they govern different lines of `pg_hba.conf`,
      # and only one of them is on PgBouncer's path.** Read out of the image's
      # `docker-entrypoint.sh` rather than assumed:
      #
      #   POSTGRES_HOST_AUTH_METHOD appends `host all all all <method>` — the
      #   catch-all line, and the one a connection from the compose network
      #   matches. This is the load-bearing one. Left unset it defaults to the
      #   server's `password_encryption`, which is already scram-sha-256 on 18;
      #   it is set anyway so that `auth_type` in `pgbouncer.ini` is agreeing
      #   with something stated rather than with an upstream default.
      #
      #   POSTGRES_INITDB_ARGS --auth-host governs the initdb-generated
      #   127.0.0.1/::1 lines, which only in-container loopback ever uses.
      POSTGRES_HOST_AUTH_METHOD: scram-sha-256
      POSTGRES_INITDB_ARGS: --auth-host=scram-sha-256
    ports:
      # **Bound to loopback, not to `0.0.0.0`.** A bare "5432:5432" publishes the
      # development database to every host on whatever network the laptop is
      # joined to — a café, a co-working space, a conference — with a password
      # that is committed to a public repository. The host-IP prefix is the
      # whole mitigation, and it is why the credentials below can honestly be
      # called non-secret.
      - "127.0.0.1:5432:5432"
    volumes:
      # `/var/lib/postgresql`, not `/var/lib/postgresql/data`. The 18 images
      # store the cluster in a major-version-specific subdirectory so that
      # `pg_upgrade --link` can cross versions without a mount-point boundary,
      # and they **refuse to start** against the old path. Verified by hitting
      # it: `docker-library/postgres` PR #1259.
      - postgres-data:/var/lib/postgresql
      # Runs once, on an empty data directory. `citext` and `pg_trgm` are what
      # seam 2 depends on (spec 0002, Testing Decisions) and what a human
      # enables by hand on PlanetScale at go-live runbook §2 — they are enabled
      # out of band on both, so they are enabled out of band here rather than in
      # a migration that could not run against PlanetScale anyway.
      - ./docker/postgres/initdb:/docker-entrypoint-initdb.d:ro
    healthcheck:
      # `-U`/`-d` so this reads the application database rather than the
      # bootstrap one, and `-q` so a passing check is silent.
      test: ["CMD-SHELL", "pg_isready -q -U recomencemos -d recomencemos"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 10s

  # The pooled connection. What the app uses, and what makes local as
  # constrained as production.
  pgbouncer:
    image: edoburu/pgbouncer:v1.25.2-p0@sha256:7d7a27d9e90985cab5cf42256f5c13a3120baa4b055b69df37beb272b89b2340
    # Run the binary against a mounted config instead of the image's
    # entrypoint, which synthesises `pgbouncer.ini` from environment variables.
    # The setting that gives this file its reason to exist is `pool_mode`, and
    # it should be readable in a file rather than inferred from an env default.
    # Overriding `entrypoint` clears the image's `command`, so nothing is
    # appended after the config path.
    entrypoint: ["/usr/bin/pgbouncer", "/etc/pgbouncer/pgbouncer.ini"]
    volumes:
      - ./docker/pgbouncer/pgbouncer.ini:/etc/pgbouncer/pgbouncer.ini:ro
      - ./docker/pgbouncer/userlist.txt:/etc/pgbouncer/userlist.txt:ro
    ports:
      # Loopback, for the reason written on the postgres service above.
      - "127.0.0.1:6432:6432"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      # PgBouncer answers a startup packet before any pool exists, so this
      # proves the listener, not the route to Postgres. `depends_on:
      # service_healthy` above is what proves the route.
      test: ["CMD-SHELL", "pg_isready -q -h 127.0.0.1 -p 6432 -U recomencemos"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s

  # The object store. Photos, in two buckets with two different lifetimes.
  object-store:
    image: versity/versitygw:v1.8.0@sha256:30292fc2eeacc67a36993b01f7a7a5e3361a19cced0e80c1d71cfa2a4b0a2499
    # Arguments given, so the image's entrypoint execs the gateway directly.
    # `--region auto` because the product signs with `auto`, which is what R2
    # specifies, and the gateway checks the region in a signature's scope.
    # `--health` is what the healthcheck below reads. `posix` stores each object
    # as a file on the volume.
    command:
      ["--port", ":9000", "--region", "auto", "--health", "/health", "--quiet", "posix", "/data"]
    environment:
      # Development-tier, loopback-bound, and in git for the reason the Postgres
      # password above is: `apps/web/.env.example` carries the same pair, and a
      # real R2 key in either file is the incident C13 names.
      ROOT_ACCESS_KEY_ID: recomencemos
      ROOT_SECRET_ACCESS_KEY: recomencemos
    ports:
      # Loopback, for the reason written on the postgres service above — and
      # here it is doing more work than there: a bucket published to a café
      # network is a bucket a stranger can PUT into with a committed password.
      - "127.0.0.1:9000:9000"
    volumes:
      # The posix backend keeps an object's metadata — its ETag and content
      # type — in extended attributes, so the volume's filesystem must support
      # user xattrs. Docker Desktop's volumes do (#299).
      - object-store-data:/data
    healthcheck:
      # The gateway's own endpoint. The image is Alpine, so busybox `wget` is
      # there as shipped; a wrong path makes `up --wait` exit 1 (#299).
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:9000/health"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s

  # Runs once, exits, and is what makes the two buckets different.
  object-store-init:
    # **Plain `curl`, not an S3 CLI** (#316). The gateway ships no client that
    # sets a bucket policy — `versitygw admin` manages accounts — and the stock
    # one that does, `amazon/aws-cli`, is 490 MB for a handful of calls. `curl
    # --aws-sigv4` signs the same requests from a 26 MB image. Measured against
    # this gateway on 2026-09-15 rather than assumed: a second run exits 0 and
    # leaves the same state, a policy set on quarantine by hand is gone after
    # the next run, and a wrong key exits 22 with the store's
    # `SignatureDoesNotMatch` printed.
    image: curlimages/curl:8.22.0@sha256:58adaa4e8dca9c988bae2aba4ab3434a0bb2da16bbe3f92dec39ec7785166777
    depends_on:
      object-store:
        condition: service_healthy
    # **Behind a profile, so `docker compose up --wait` never starts it.**
    # `--wait` reports any container that stops as a failure, exit code 0
    # included, unless another service depends on it with
    # `service_completed_successfully` — and nothing does, because nothing in
    # this file is the app. Measured on Compose v5.5.1, with the init service
    # this one replaced: every `pnpm db:up` exited 1 reporting the init
    # container `exited (0)` while all three servers were healthy and both
    # buckets were set. `pnpm db:up` runs this with `docker compose run --rm
    # object-store-init` instead, which targets the service by name (so the
    # profile needs no `--profile`) and returns the container's own exit code.
    # A bare `docker compose up` therefore creates no buckets — use `pnpm
    # db:up`. `pnpm db:down` and `db:reset` pass `--profile init` for the other
    # direction: `down` skips a service whose profile is inactive, so an
    # `object-store-init-1` left by an earlier `up` would outlive it and hold
    # the network open.
    profiles: ["init"]
    # **A bucket policy granting anonymous `s3:GetObject` on the whole photos
    # bucket, and no policy at all on the quarantine one.** That asymmetry is
    # the point of this service, and it is stated at the *bucket* rather than
    # at a prefix because that is the only place R2 can state it: public access
    # there is one switch per bucket, with no per-prefix policy and no
    # S3-style bucket policy behind it (#251). A prefix-scoped rule here would
    # be a local capability production has no way to reproduce — which is the
    # failure a fixture is supposed to prevent rather than commit. Runbook §3
    # is these same acts performed by a human.
    #
    # **The grant is `s3:GetObject` and nothing else, so nothing lists.** An R2
    # public bucket serves objects and never a listing, and so does this one:
    # an anonymous `GET /recomencemos-photos?list-type=2` answers 403, measured
    # 2026-09-15. Do not widen the grant to listing, and do not narrow it back
    # to a prefix — #251 is why the second would be the worse infidelity.
    #
    # **Quarantine also answers the browser's CORS preflight**, from
    # `docker/object-store/cors.xml`: `PUT` with `content-type` and
    # `if-none-match`, from the main checkout's origin, any branch's, and
    # `http://localhost:3000` for `PORTLESS=0`. Measured 2026-09-15: without it
    # every preflight is a 403 and the publish surface uploads nothing, while
    # the storage suite stays 23/23 because Node sends no preflight. With it,
    # those origins get 200, and a look-alike origin, an unlisted header or a
    # `DELETE` still get 403. **Runbook §3 names no CORS rule for R2**, and an
    # R2 bucket has none by default, so production's half of this is a row in
    # the README's "Still to replace" rather than settled in a development file.
    #
    # `docker/object-store/init.sh` is idempotent, so this is safe to re-run
    # on every `pnpm db:up` rather than only on a fresh volume.
    environment:
      ENDPOINT: http://object-store:9000
      ACCESS_KEY_ID: recomencemos
      SECRET_ACCESS_KEY: recomencemos
    volumes:
      - ./docker/object-store/init.sh:/init.sh:ro
      - ./docker/object-store/public-read.json:/public-read.json:ro
      - ./docker/object-store/cors.xml:/cors.xml:ro
    entrypoint: ["/bin/sh", "/init.sh"]

volumes:
  # Named, so `pnpm db:down` keeps the data and `pnpm db:reset` is the deliberate
  # act that discards it.
  postgres-data:
  # Same treatment, and it matters more here: a photo is bytes nothing else
  # holds a copy of, so a `down` that discarded them would lose whatever a
  # session had uploaded to look at.
  object-store-data:
